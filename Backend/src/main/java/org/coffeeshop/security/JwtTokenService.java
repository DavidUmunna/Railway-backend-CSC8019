package org.coffeeshop.security;

/**
 * Deprecated legacy JWT helper kept only to avoid breaking old references.
 * New code should use {@link JwtService}.
 */
@Deprecated
public class JwtTokenService {
    private JwtTokenService() {
    }
}
